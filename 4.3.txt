#include <stdio.h>
#include <locale.h>
#include <math.h>

int main()
{
    int n,i,k=0;
    float a;
    printf("a=");
    scanf("%f",&a);
    printf("n=");
    scanf("%d",&n);
    for(i==0; i<=n; i++)
    {
    k+=i;
    }
    a+=k;
    printf("%g",a);
    return 0;
}